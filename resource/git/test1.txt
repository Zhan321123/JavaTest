测试git命令
//初始化文件位置
git init
//初始化全局用户
git config --global user.name "username"
git config --global user.email "you@email.com"

//增加事务
git add "file name"
//提交事务
git commit -m "name"

//查看日志
git log
//清屏
clear